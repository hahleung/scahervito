package kyu4

object TwiceLinear {
  def firstLinearResult(x: Int) = 2 * x + 1
  def secondLinearResult(x: Int) = 3 * x + 1

  def dblLinear(n: Int): Int = {
//    stream1.sortWith(_ > _).take(1).head
    2
  }
}
